class Repo {
  String title;
  String owner;
  String img;
  String description;
  int forks;
  int stargazers;
  int watchers;

  Repo(this.title, this.owner, this.img, this.description, this.forks,
      this.stargazers, this.watchers);
}
